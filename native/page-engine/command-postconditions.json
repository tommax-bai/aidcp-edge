{
  "schemaVersion": 1,
  "purpose": "Native 写命令的**后置校验盘点**（change restore-native-actuation-humanization-and-locating，任务 5.6 / 5.11）。规格里「每一条写命令都必须校验其效果」这条保证，在这张表存在之前**没有任何人说得清每条命令落在线的哪一侧**——所以它只能按「已逐条核过的面」读，不能当成整个运行时的属性。本表就是那份逐面盘点。",
  "invariants": [
    "本表 MUST 恰好覆盖引擎命令类型里 may_write 为真的全部命令，一条不多一条不少。**没列出的命令会被读成已覆盖**——这正是本表要消除的那种沉默。",
    "status 只有四种：confirmed=后置判据达标；below_bar=有判据但不达 5.9/5.10 的强度条（须写清差在哪）；not_applicable=结构上没有可读回的业务结果（须写清为什么，且不得因此默认成功）；unread=本轮没逐条读过。",
    "unread 是**单调棘轮**：unreadBudget 只许下降，绝不许上调。新增写命令时它的默认归宿不是 unread——新命令必须当场分类，否则这张表会退化成一个永远填不完的许愿单。",
    "below_bar 与 unread 都 MUST NOT 被读成「有校验」。前者是已知不够，后者是没人核过——两者都不构成「这条命令不会假成功」的证据。",
    "below_bar 的棘轮与 unread **同构**：belowBarBudget 恰等于实际条数、不留空位、只许下降。同构是刻意的——两个形态不同的棘轮会让「哪个方向被锁住了」变成需要读代码才知道的事。",
    "**只锁 unread 一个方向是不够的**：把一条 unread 改成 below_bar、附一句「差在哪」，unreadBudget 依法下降、门禁全绿，而那条命令的假成功风险一点没变。所以 below_bar 每条 MUST 带 disposition（具名处置），让这条路要么真消除、要么当场留下「谁来解、卡在什么前置上」。",
    "disposition 两种形态：kind=fix 须写 action（消除动作）与 owner（谁来做）；kind=exception 须写 reason（为什么现在做不到）、blockedBy（前置是什么）与 owner（前置解除后谁来解）。**没有处置的 below_bar 与 confirmed 的区别只存在于读者的记忆里**，而这张表存在的全部理由就是不再依赖任何人的记忆。"
  ],
  "unreadBudget": 0,
  "belowBarBudget": 0,
  "commands": [
    {
      "nativeKind": "facebook_auth_submit_login",
      "status": "confirmed",
      "evidence": "点击后按**导航代际 + 候选指纹**复核（`facebook/auth.rs:797` verify_postcondition → `facebook-router/06-auth.js:392` authPostcondition）：文档代际变了（timeOrigin+URL 摘要，只有真换文档才变，页内重渲染不变）即算生效；否则重新结构化观测该信号，按候选指纹判它是否已消失。**读不出来时 determinate=false，绝不折成成功、也绝不折成「确实没发生」**，有界轮询到顶回 Ambiguous，且 unconfirmed 与 unreadable 两个原因码分开。 本条走 `execute_click`（`auth.rs:154`），期望信号 login_submit_ready。"
    },
    {
      "nativeKind": "facebook_auth_enter_totp",
      "status": "confirmed",
      "evidence": "录入后按**元素指纹绑定 + 精确相等**回读（`auth.rs:310` read_totp → `06-auth.js:376` authTotpReadback）：要求 bound（读的是同一个输入框）+ matches（值与下发码逐字相等）+ length===6 + 非空，四者缺一即 Ambiguous；读不到回 Ambiguous 的独立原因码。**不属「自证」禁令覆盖面**——该禁令针对「业务结果是平台接受了提交」那类（规格 native-locating-gates 明写，且把 mention/location/collection 具名为例外）；本条的业务结果就是「码进了那个框」，受控输入框会拒改或规范化，精确回读确实证明了它。"
    },
    {
      "nativeKind": "facebook_auth_submit_totp",
      "status": "confirmed",
      "evidence": "点击后按**导航代际 + 候选指纹**复核（`facebook/auth.rs:797` verify_postcondition → `facebook-router/06-auth.js:392` authPostcondition）：文档代际变了（timeOrigin+URL 摘要，只有真换文档才变，页内重渲染不变）即算生效；否则重新结构化观测该信号，按候选指纹判它是否已消失。**读不出来时 determinate=false，绝不折成成功、也绝不折成「确实没发生」**，有界轮询到顶回 Ambiguous，且 unconfirmed 与 unreadable 两个原因码分开。 本条先校验 TOTP 时间窗仍新鲜（`auth.rs:378`，不新鲜即 NotStarted），再走 `execute_click_with_observation`（`auth.rs:539`）。"
    },
    {
      "nativeKind": "facebook_auth_clear_totp",
      "status": "confirmed",
      "evidence": "清空后按**元素指纹绑定 + 空值**回读（`auth.rs:507`）：要求 bound + empty + length===0；读不到 / 不匹配各有独立原因码回 Ambiguous。键入中途被取消或派发失败**回 Ambiguous 而非 NotStarted**（`auth.rs:456`/`:497`），因为退格已经打出去了。"
    },
    {
      "nativeKind": "facebook_auth_dismiss_warning",
      "status": "confirmed",
      "evidence": "点击后按**导航代际 + 候选指纹**复核（`facebook/auth.rs:797` verify_postcondition → `facebook-router/06-auth.js:392` authPostcondition）：文档代际变了（timeOrigin+URL 摘要，只有真换文档才变，页内重渲染不变）即算生效；否则重新结构化观测该信号，按候选指纹判它是否已消失。**读不出来时 determinate=false，绝不折成成功、也绝不折成「确实没发生」**，有界轮询到顶回 Ambiguous，且 unconfirmed 与 unreadable 两个原因码分开。 本条走 `execute_click`，期望信号 automation_warning_dismiss。"
    },
    {
      "nativeKind": "facebook_auth_close_push_blocker",
      "status": "confirmed",
      "evidence": "点击后按**导航代际 + 候选指纹**复核（`facebook/auth.rs:797` verify_postcondition → `facebook-router/06-auth.js:392` authPostcondition）：文档代际变了（timeOrigin+URL 摘要，只有真换文档才变，页内重渲染不变）即算生效；否则重新结构化观测该信号，按候选指纹判它是否已消失。**读不出来时 determinate=false，绝不折成成功、也绝不折成「确实没发生」**，有界轮询到顶回 Ambiguous，且 unconfirmed 与 unreadable 两个原因码分开。 本条走 `execute_click`，期望信号 push_blocker_close。"
    },
    {
      "nativeKind": "facebook_auth_confirm_remember_password",
      "status": "confirmed",
      "evidence": "点击后按**导航代际 + 候选指纹**复核（`facebook/auth.rs:797` verify_postcondition → `facebook-router/06-auth.js:392` authPostcondition）：文档代际变了（timeOrigin+URL 摘要，只有真换文档才变，页内重渲染不变）即算生效；否则重新结构化观测该信号，按候选指纹判它是否已消失。**读不出来时 determinate=false，绝不折成成功、也绝不折成「确实没发生」**，有界轮询到顶回 Ambiguous，且 unconfirmed 与 unreadable 两个原因码分开。 本条走 `execute_click`，期望信号 remember_password_confirm。"
    },
    {
      "nativeKind": "facebook_auth_start_ad_data_review",
      "status": "confirmed",
      "evidence": "本条只接受新鲜的 `ad_data_review_get_started` 候选并走 Native `execute_click`（`facebook/auth.rs:160-170`）；派发后由 `verify_postcondition`（`:840-897`）有界复核。路由器仅在精确的订阅或免费广告选择后继结构出现时设置 `successorObserved + satisfied + signalGone`（`facebook-router/06-auth.js:478-499`），Native 才回 Confirmed。只有按钮变化、加载迹象、文档变化或读回失败均不算成功，分别保持有界等待或回 `auth_successor_unconfirmed` / `auth_postcondition_unreadable` 的 Ambiguous。"
    },
    {
      "nativeKind": "facebook_auth_start_suspension_appeal",
      "status": "confirmed",
      "evidence": "本条只接受新鲜的 `suspension_appeal_start` 候选并走 Native `execute_click`；识别要求精确 Facebook 数字 checkpoint、规范 next 目标、完整停用/申诉正文，以及唯一可见、启用、topmost 的 `Appeal`，现场隐藏 disabled 克隆被 visibility 门禁排除。派发后由 `verify_postcondition` 有界复核；只有原停用入口消失且同一 Facebook 数字 checkpoint 已完整、非空、无 loading 地进入不同步骤时才设置 `successorObserved + satisfied + signalGone`。按钮变化、loading、任意非 checkpoint 导航或读回失败均不算成功，回 `auth_successor_unconfirmed` / `auth_postcondition_unreadable` 的 Ambiguous，且 signal id 消费后不重放。"
    },
    {
      "nativeKind": "plan_execute",
      "status": "confirmed",
      "evidence": "两支都有实测判据：page.scroll 按**实测位移**回报（`xhs-command-router.js:777-784`，无位移即 no_scroll，不再无条件 success）；点击支用与主路径**同源的硬判据** compatVerifier（图标状态位 / 关注文案 / 焦点落位，`:806`），有界重试，且区分「点了但没确认」(escalated) 与「根本没点上」(no_target)，attempts 如实回报。输入支带**自证闸**：非可编辑元素直接判 no_target（`:790`）——在 div 上 dispatchInput 会改 textContent、回读必然相等，那不是证据。⚠️ **本条在活路径上已死**（云端 createEnvelope 出口没有它，唯一产出方是云端联调脚本）：这条分类是「读过了、判据够强」，不是「它在跑」。"
    },
    {
      "nativeKind": "session_stop",
      "status": "not_applicable",
      "evidence": "它不在页面上产生任何业务效果，没有可读回的业务结果。**不得因此默认成功**——它回报的是本地会话收尾是否完成"
    },
    {
      "nativeKind": "browse_scroll",
      "status": "confirmed",
      "evidence": "引擎内部载体：首帖探测（facebook/runtime.rs）构造它驱动群容器内滚动；协议侧 browse.scroll 已随批 1 删除、envelope 不可达。词汇批 4 核实后保留（删除会断 FB 群评论链）。"
    },
    {
      "nativeKind": "page_scroll",
      "status": "confirmed",
      "evidence": "同上"
    },
    {
      "nativeKind": "feed_refresh",
      "status": "confirmed",
      "evidence": "点前记批次首部的 id 序列（cards() 本就带 noteId，取前 5 条）、点后有界轮询 3s / 150ms 等它变；没变即 ambiguous + refresh_batch_unchanged，读不到卡片即 refresh_cards_unreadable，两态分开、**都不回成功**。⚠️ **本条此前记的 confirmed 是错的**：当时写「刷新后重扫卡片，按实测结果回报」，而实现是点完睡 900ms 无条件回当前卡片、全程不取基线也不校验换没换（缺口登记 E13）。**同一张表里既有「登记比代码旧」也有「登记比代码好看」**，两种都只能靠实读判据代码发现，不能靠读这一栏。"
    },
    {
      "nativeKind": "search_execute",
      "status": "confirmed",
      "evidence": "关键词 / 结果路由确认到位；**登记里那条「归因折叠」已经被修掉了，登记是照抄旧文没回头读代码**。实读现状：几何探针与状态探针都先过 `evaluated_value`（`engine.rs:1026` / `:2688`），页内异常一律成 Err、不再落到 `xhs_search_input_flag` 的 `unwrap_or(false)` 上被读成「页面上没有搜索框」；打字后的回读失败另有独立原因码 `search_input_readback_failed`（`engine.rs:1103`），与 `readback_mismatch`（确实读到了不一样）分列。修它的是 `aidcp-edge bfaa33d`（2026-07-29，change restore-native-xiaohongshu-session-guards，2026-08-01 归档），提交说明与新增注释逐字点名的就是这条折叠。**比本表建表（fcb1fd2，08-01）还早两天** —— 原登记（该 change §9.5-1）自己就写着「另一条流可能正压着这块」，那条流已经压完了。剩余的 `unwrap_or(false)` 只在「页内脚本返回结构漂移」这一假想态下才折叠，而该脚本（xhs-search-input-geometry.js:28/41）每条返回分支都带齐 found / focused / value；**为一个假想态去动 engine.rs 这个并行热点文件不划算**，不改，如实记在这里。"
    },
    {
      "nativeKind": "note_open",
      "status": "confirmed",
      "evidence": "详情容器 + 笔记 id 一致；错误页 / 访问限制词库先行否决，地址解析得出 id 不算打开成功"
    },
    {
      "nativeKind": "note_close",
      "status": "confirmed",
      "evidence": "有界轮询确认浮层已关"
    },
    {
      "nativeKind": "navigation_back",
      "status": "confirmed",
      "evidence": "有界轮询确认已回到列表面"
    },
    {
      "nativeKind": "note_browse_images",
      "status": "confirmed",
      "evidence": "图序标记（轮播激活片下标，退到首张可见图地址）前后比对；复制片不计真图"
    },
    {
      "nativeKind": "note_scroll_comments",
      "status": "confirmed",
      "evidence": "实测位移 + 可见评论行数；行数读不到回「读不到」，绝不顶成 1"
    },
    {
      "nativeKind": "profile_open",
      "status": "confirmed",
      "evidence": "点击前按地址里的 authorId **精确绑定目标**（`xhs-command-router.js:629`，找到的不是指定作者即 fail profile_target_mismatch，绝不点下去）；点击后复核地址确实落在 /user/profile/ 且含该 authorId，不满足回 ambiguous profile_navigation_unconfirmed。已有行为契约用例把「跳到了主页」与「跳到了那个人的主页」分成两条（`test/native-page-engine/xhs-navigation-command-contracts.test.ts`）。"
    },
    {
      "nativeKind": "notification_open",
      "status": "confirmed",
      "evidence": "有界轮询确认地址真落到通知面（`xhs-command-router.js:641` waitFor 1800/120），不落回 ambiguous notification_navigation_unconfirmed；**并额外要求分类栏可读**（`:644`，读不到回 ambiguous notification_tabs_not_found）——这一条正是为「读不到分类栏」与「三栏都是 0」必须可区分而加：云端把计数 ≤0 当该类已清零跳过，读不到时若回一份全 0 读数，整条巡视会静默什么都不做。"
    },
    {
      "nativeKind": "notification_browse_comments",
      "status": "confirmed",
      "evidence": "点击分类栏后按**平台产出的**证据确认「现在就在这一类上」，三条任一成立即可（`xhs-command-router.js` 的 notification_browse_* 分支）：① 叶子分类栏处于激活态（读共享三态判据 stateOf——整词分段 + 否定形拒绝 + 读不到如实回读不到；**只读叶子**，包裹容器带同样类名但不算数，有专门用例钉住）；② 该类未读角标由非零归零；③ 可见列表换了。有界轮询 2.5s / 150ms，三条都证不出来即 not_started + notification_category_unconfirmed，**绝不记成已清零**。评论类另加一道：没确认切栏就**不回 items**（云端凭 items 到达即结案，栏没切时读到的是另一类的行）。"
    },
    {
      "nativeKind": "notification_browse_likes",
      "status": "confirmed",
      "evidence": "点击分类栏后按**平台产出的**证据确认「现在就在这一类上」，三条任一成立即可（`xhs-command-router.js` 的 notification_browse_* 分支）：① 叶子分类栏处于激活态（读共享三态判据 stateOf——整词分段 + 否定形拒绝 + 读不到如实回读不到；**只读叶子**，包裹容器带同样类名但不算数，有专门用例钉住）；② 该类未读角标由非零归零；③ 可见列表换了。有界轮询 2.5s / 150ms，三条都证不出来即 not_started + notification_category_unconfirmed，**绝不记成已清零**。评论类另加一道：没确认切栏就**不回 items**（云端凭 items 到达即结案，栏没切时读到的是另一类的行）。"
    },
    {
      "nativeKind": "notification_browse_follows",
      "status": "confirmed",
      "evidence": "点击分类栏后按**平台产出的**证据确认「现在就在这一类上」，三条任一成立即可（`xhs-command-router.js` 的 notification_browse_* 分支）：① 叶子分类栏处于激活态（读共享三态判据 stateOf——整词分段 + 否定形拒绝 + 读不到如实回读不到；**只读叶子**，包裹容器带同样类名但不算数，有专门用例钉住）；② 该类未读角标由非零归零；③ 可见列表换了。有界轮询 2.5s / 150ms，三条都证不出来即 not_started + notification_category_unconfirmed，**绝不记成已清零**。评论类另加一道：没确认切栏就**不回 items**（云端凭 items 到达即结案，栏没切时读到的是另一类的行）。"
    },
    {
      "nativeKind": "notification_back_home",
      "status": "confirmed",
      "evidence": "这里的「首页」是**通知页的分类栏那一层**，不是信息流首页——云端一类看完后靠这一步重报的三栏未读挑下一类。与 notification_open 共用同一份实现（`xhs-command-router.js` 的 enterNotificationHome），后置＝落在通知页 URL 上 + 分类栏可读，终局是三栏未读读数；不在通知页时经通知入口进入，找不到入口 / 点不动 / 有界等待 1.8s 后 URL 仍不是通知页 分别回 notification_entry_not_found / _not_actuated / notification_navigation_unconfirmed；落到通知页但分类栏 1.5s 内读不到 → ambiguous + notification_tabs_not_found，**绝不回一份全 0 的三栏读数**（云端把计数 ≤0 当作该类已清零）。由 `xhs-navigation-command-contracts.test.ts` 锁住，含一条「页面上有首页入口与信息流卡片时也不得回信息流」的回归守卫。**它是出口命令**：假成功不体现在自己身上，体现在它之后的每一条命令上——2026-08-05 的线上故障正是它导航到了 /explore 并回一屏卡片，云端等不到重读结果，巡视永久挂起而每层回执都仍是成功的。"
    },
    {
      "nativeKind": "interaction_like",
      "status": "confirmed",
      "evidence": "小红书：有界轮询等图标状态位翻转；否定形类名不算正证据；Facebook：两个浏览面各自三态（Confirmed / Unchanged / Indeterminate）；Reels 面身份退化分支走三道闸编排，探针读不到绝不塌成「没点上」"
    },
    {
      "nativeKind": "interaction_collect",
      "status": "confirmed",
      "evidence": "同上"
    },
    {
      "nativeKind": "interaction_follow",
      "status": "confirmed",
      "evidence": "小红书：有界轮询等关注态文案 / 按下态；Facebook：关注态探针前后比对，带目标帖身份"
    },
    {
      "nativeKind": "interaction_comment",
      "status": "confirmed",
      "evidence": "小红书：两条独立证据：正文出现在评论区 + 编辑器已被平台清空；扫描面剔除编辑器及其容器（防自证）；有界轮询 8×250ms；Facebook：评论到达探针（按帖 + 文本 + 账号身份）"
    },
    {
      "nativeKind": "interaction_like_comment",
      "status": "confirmed",
      "evidence": "有界轮询等图标状态位翻转（原为固定睡眠后单次采样）"
    },
    {
      "nativeKind": "group_join",
      "status": "confirmed",
      "evidence": "有界轮询复查加入态"
    },
    {
      "nativeKind": "identity_read_self_profile",
      "status": "confirmed",
      "evidence": "直达自己主页后先等页面类型确实是 Profile（`engine.rs:810` wait_for_page_kind 5s，不到即 navigation_postcondition_failed），再复用 ProfileOpen 那条带 authorId 精确绑定的路径取资料（`engine.rs:813`），拿不到 ProfileDetail 即报错。**两道都是页面自己产出的结构信号**，不是把下发的 accountId 回显一遍。"
    },
    {
      "nativeKind": "captcha_click",
      "status": "confirmed",
      "evidence": "焦点落位分档（editable / opaque / none）+ 键入取证 + 清除结果三态；回放模式按实际路径派生（本波 2.10）"
    },
    {
      "nativeKind": "publish_navigate_entry",
      "status": "confirmed",
      "evidence": "导航后等页面类型确实是 Publish（`engine.rs:911` wait_for_page_kind 6s）；不到**不报成功**，回 Ambiguous + publish_entry_unconfirmed（`engine.rs:913-925`）。"
    },
    {
      "nativeKind": "publish_select_mode",
      "status": "confirmed",
      "evidence": "ready() 结构判据：图文编辑区真的出现"
    },
    {
      "nativeKind": "publish_upload_image",
      "status": "confirmed",
      "evidence": "判据已绑定**本次**上传：写文件之前先取一份预览位身份基线，写之后有界轮询（5s / 250ms）等目标序号位出现**基线里没有的**身份（`engine.rs` 的 execute_xhs_publish_upload_image → 分片 `xhs-input-targets.js` 的 publish_previews，身份取图片地址的长度 + FNV-1a 摘要、不外传地址本身）。残留预览不再满足判据。四种终局各有独立原因码、一条都不压成另一条：基线读不到（**在写文件之前**停手，回 not_started）、那一位一直没出现、出现了但身份读不出来、出现了但还是基线里那张（后三者均 ambiguous）。身份缺席时按「读不出来」算（悲观默认），绝不当成新图。脱机用例 `native/page-engine/tests/xhs_publish_upload_binding.rs` 5 例，三次变异分别由三条不同用例抓住。"
    },
    {
      "nativeKind": "publish_set_cover",
      "status": "confirmed",
      "evidence": "封面专属证据（data-cover 等），有界回溯只认真正的图片项容器；轮播「当前显示」与点后选中外观都不算"
    },
    {
      "nativeKind": "publish_fill_field",
      "status": "confirmed",
      "evidence": "有界回读：空白归一 / 汉字档 / 段落数三道比对，段落数读不到不放行"
    },
    {
      "nativeKind": "publish_add_with_candidate",
      "status": "confirmed",
      "evidence": "所有成功出口均达到强度条或被 fail-closed：topic 由 Rust 特化按正文里的真话题 token + 剔隐藏后缀 + 精确相等确认；mention / location / collection 在真机结构信号完成标定前没有 confirmed 出口，找不到编辑器/入口/候选或点不着均回具体 not_started，候选点击已派发后固定回 ambiguous + publish_candidate_unconfirmed。正文中的 @ 字面值、候选自身 selected 外观与入口文案回显均不再构成成功证据。"
    },
    {
      "nativeKind": "publish_set_option",
      "status": "confirmed",
      "evidence": "有界轮询等开关状态位；读不到明确状态一律去点，删掉了「否 === 否」那条兜底"
    },
    {
      "nativeKind": "publish_set_schedule",
      "status": "confirmed",
      "evidence": "先读真实开关三态：已开幂等、已关才点击并有界等翻转、读不到不猜；写入后同一窗口内必须同时确认开关仍开、Asia/Shanghai 目标分钟精确相等、唯一精确 `定时发布` 叶子提交控件存在。Native 会话只在这三证成功后保存 scheduled(target minute)，submit 前再复读模式与分钟。"
    },
    {
      "nativeKind": "publish_submit",
      "status": "confirmed",
      "evidence": "模式由 Native 会话真态绑定并在点击前复读，unknown / 漂移零点击且 submitDispatched=false；点击后只认结果 / 提示作用域内相对点击前节点+文本基线新增的终态成功文案。地址、全页正文、进行态 `发布中` 与陈旧 toast 都不确认；立即发布身份也只从该 fresh success 作用域的唯一详情候选提取，由 Native session 按 recordId 绑定，后续 capture 不再全页猜旧帖。命令记录写入后若 transport 丢终局，宿主保守回 submitDispatched=true，禁止安全重投。"
    }
  ]
}
