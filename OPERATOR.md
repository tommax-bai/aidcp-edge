# AIDCP Edge 运维手册（桌面应用）

面向运维：如何在 macOS / Windows 上安装、登录、运行 AIDCP Edge 桌面应用，以及常见问题排查。

> 一句话定位：本应用是边缘端「执行壳」——它启动并接管一个真实 Chrome、连接云端、把云端指令在小红书页面上执行，并在托盘提供状态/暂停/恢复/重登。规划、风控、编排都在云端，不在本机。

## 0. 前置要求（务必先满足）

- **必须先安装 Google Chrome**。本应用**不捆绑 Chrome**，运行时依赖系统已安装的 Chrome；找不到会明确报错并弹窗提示「请安装 Chrome」，不会静默装作在运行。
- 一台机器需要能访问云端地址（默认生产 `ws://121.89.85.150:8787`）。
- 内存：本应用是 Electron（约 200MB）+ 它另起的真实 Chrome（约 500–800MB）。**每开一个实例 ≈ 1GB**；要在一台机器上同时开 N 个，请按 ~1GB/个预留内存。

## 1. 安装

### macOS
1. 下载 `AIDCP-<version>-<arch>.dmg`（`arch` 选 `arm64`=Apple Silicon / `x64`=Intel）。
2. 打开 dmg，把 `AIDCP` 拖进「应用程序」。
3. **首次打开需手动放行**（应用未做苹果公证）：在「应用程序」里**右键点 AIDCP → 打开 → 再点「打开」**。（直接双击会被 Gatekeeper 拦「无法验证开发者」，右键打开可绕过，仅首次需要。）

### Windows
1. 下载并运行 `AIDCP Setup <version>.exe`（nsis 安装包，x64）。
2. **首次运行 SmartScreen 可能提示**（应用未做代码签名）：点「更多信息 → 仍要运行」。（仅首次。）

> 说明：内部分发暂不做代码签名 / 公证，故首次需手动放行；这是预期行为，不是病毒告警。

## 2. 首次登录

1. 启动 AIDCP。应用会自动拉起一个 Chrome 窗口并打开小红书。
2. 在该 Chrome 窗口里**扫码登录你要运营的小红书账号**。
3. 登录成功后，应用自动从登录态认出账号身份、连接云端并开始工作。状态面板的「登录 / 云端连接」会转绿。

> 账号身份来自「你在这个窗口里登录的那个号」，不需要手填账号。换号 = 在这个窗口登出再登另一个号。

## 3. 一台机器开多个（多开）

当前形态：**一个应用实例 = 一个账号 = 一个 Chrome = 一条云连接**。

> ⚠️ **重要：多开隔离尚未实现（在另一个变更 `account-identity-from-login` 中处理）。** 在隔离完成之前，**不要在同一台机器上同时开多个本应用实例**——多个实例会争用同一个调试端口与同一个 Chrome 用户目录，第二个实例可能**接管第一个账号的浏览器**，导致串号。需要同时跑多个账号时，请等隔离能力上线，或暂时用每台机器一个账号。

## 4. 状态面板 & 日常操作

托盘图标点开是状态面板，含：
- **登录 / 云端连接 / 会话 / 风控 / 边缘进程** 五个状态。
- **暂停 / 恢复**：临时停/继续自动浏览。
- **重新登录**：登录态失效时重新扫码。

正常运行时面板各项应为「已连接 / 运行中」。

## 5. 排查

- **面板停在「starting」或「disconnected」**：边缘进程可能已退出或连云失败。本应用在**异常退出 / Chrome 缺失 / 连云失败**时会主动把窗口弹到前台 + 发系统通知——看到通知说明确实出问题了，不是「在后台默默跑」。按提示处理（装 Chrome / 检查网络到云端 / 重新登录）后重启应用。
- **「Chrome 未找到」**：装 Google Chrome 后重启应用。
- **连不上云端**：确认本机能访问云端地址、云端在线。应用是「快速失败 + 弹窗告知」，**不会无限重连假装在跑**；处理后重启即可。
- **崩溃后看不到原因**：打开窗口，最近一条 `lastMessage` 即最后的错误行。

## 6. 已知限制（当前版本）

- 未做代码签名 / 公证（首次需手动放行，见 §1）。
- 未做自动更新：升级 = 重新下载安装新版本。
- 多开隔离未做（见 §3），暂不要同机多开。
- 不打包 Chrome（依赖系统 Chrome，见 §0）。
