# AIDCP Edge 运维手册（桌面应用）

面向运维：如何在 macOS / Windows 上安装、登录、运行 AIDCP Edge 桌面应用，以及常见问题排查。

> 一句话定位：本应用是边缘端「执行壳」——它启动并接管一个浏览器、连接云端、把云端指令在小红书页面上执行，并在托盘提供状态/暂停/恢复/重登。规划、风控、编排都在云端，不在本机。

## 0. 前置要求（务必先满足）

- **选一个浏览器 provider**（应用内可切，界面默认 **AdsPower**）：
  - **AdsPower 指纹浏览器（默认，推荐用于多账号防关联）**：每个「分身」独立指纹 + 独立 IP + 独立登录态，规避同机多账号被平台关联。前提是本机装好 **AdsPower 客户端**并**开启其本地 API**；界面里有「下载 AdsPower」按钮直达官网。**无需另外装 Chrome**。
  - **本机 Chrome（self）**：自起系统已装的 Google Chrome。**这条路径必须先装 Google Chrome**（应用不捆绑 Chrome，找不到会明确报错弹窗，不会静默装作在运行）。
- 一台机器需要能访问云端地址（默认生产 `ws://121.89.85.150:8787`）。
- 内存：本应用是 Electron（约 200MB）+ 它接管的浏览器（Chrome / AdsPower 分身约 500–800MB）。**每开一个实例 ≈ 1GB**；同机同时开 N 个请按 ~1GB/个预留内存。

## 1. 安装

### macOS
1. 下载 `AIDCP-<version>-<arch>.dmg`（`arch` 选 `arm64`=Apple Silicon / `x64`=Intel）。
2. 打开 dmg，把 `AIDCP` 拖进「应用程序」。
3. **首次打开需手动放行**（应用未做苹果公证）：在「应用程序」里**右键点 AIDCP → 打开 → 再点「打开」**。（直接双击会被 Gatekeeper 拦「无法验证开发者」，右键打开可绕过，仅首次需要。）

### Windows
1. 下载并运行 `AIDCP Setup <version>.exe`（nsis 安装包，x64）。
2. **首次运行 SmartScreen 可能提示**（应用未做代码签名）：点「更多信息 → 仍要运行」。（仅首次。）

> 说明：内部分发暂不做代码签名 / 公证，故首次需手动放行；这是预期行为，不是病毒告警。

## 2. 选择浏览器（面板内「浏览器」设置）

面板上方有「浏览器」设置，两个选项一键切换，**默认 AdsPower**：

### 2A. AdsPower（默认）
1. 先装好 AdsPower 客户端（面板里点「下载 AdsPower」直达官网），并在 AdsPower 里**开启本地 API**、准备好**浏览器环境（分身 / profile）**。
2. 在面板「浏览器」里选 **AdsPower 指纹浏览器**。看「**AdsPower 状态**」徽标（切到 AdsPower / 点「检测」时自动探）：
   - **已就绪** = 客户端在运行且本地 API 已开；此时面板会**自动列出**你的浏览器环境（无需先点刷新）。
   - **未就绪** = 没装 / 没开客户端 / 没开本地 API——按提示启动 AdsPower 并开启本地 API（或点「下载 AdsPower」），仍可打开「手动填写」填分身 ID 继续。
3. 在**浏览器环境**列表里**点选一个**：其分身 ID（user_id）会自动带到下方「分身 ID」。每行显示名称 / 序号 / 分组 / 代理配置，方便核对「一个环境一个独立 IP 一个账号」。列表变了点「**刷新**」更新。
   - 想手填？打开「**手动填写**」开关，自己敲分身 ID（拉不到列表时的兜底）；关掉开关则回到用选中的环境。
   - 开了 API 校验？点开「**高级设置**」填 **API Key**（和 API 地址都在这里，选填、默认收起）。填了当次刷新即生效、无需先保存。
   - 没有想要的环境？点「**打开 AdsPower 新建环境**」——会尝试拉起 AdsPower 客户端（拉不起则打开官网），你在 AdsPower 里点「新建浏览器」配好后，回面板点「刷新」即可看到。
4. 点窗口**右下角的「启动」**按钮（会**自动先保存**你的设置再启动），应用会通过 AdsPower 拉起该环境的浏览器并打开小红书。**没有独立「保存」按钮**——设置在启动时自动存。
   - 若浏览器已在跑、你又改了设置：会出现「**按新设置重启**」按钮，点它应用改动（它会**先保存再重启**）；不改动 / 没在跑则不出现。
5. 若该环境**还没登录**小红书：在弹出的 AdsPower 浏览器窗口里扫码登录，然后回面板点「**重新登录**」重试。登录后应用自动认出账号身份、连云端并开始工作。

> 缺分身 ID 时，面板会显示「待配置」并提示先选环境 / 填分身 ID，**不会静默假装在跑**。
> 「分身 ID」是 AdsPower 的 **user_id**（点选环境会自动填对）；别把 AdsPower 里那个短的「序号（serial number）」当分身 ID 填，那个起不来。
> 「打开 AdsPower 新建环境」只能帮你打开客户端 + 引导，**不能**直达它内部的「新建浏览器」页（AdsPower 未提供此入口）。

### 2B. 本机 Chrome（self）
1. 面板「浏览器」里选 **本机 Chrome**（本模式无需配置），点窗口右下角「**启动**」。
2. 应用自动拉起一个 Chrome 窗口并打开小红书；在该窗口里**扫码登录**你要运营的账号。
3. 登录成功后应用自动认出账号身份、连云端并开始工作。

> 两种模式的账号身份都来自「你在那个浏览器里登录的那个号」，不需要手填账号。换号 = 在对应浏览器里登出再登另一个号（AdsPower 模式换环境则在列表里点另一个，再点右下角「启动 / 按新设置重启」，会自动保存）。

## 3. 一台机器开多个（多开）

当前**单应用实例**形态：**一个应用实例 = 一个账号 = 一个浏览器 = 一条云连接**。

> ⚠️ **多开隔离尚未在桌面外壳实现，应用会主动拒绝同机第二个实例**（弹「AIDCP Edge 已在运行」并退出），避免第二个实例接管第一个账号的浏览器导致账号串用。需要同机并行多账号时：走命令行多节点启动器（`scripts/launch-multinode.ts`，支持 AdsPower 每分身一节点），桌面外壳暂按单实例用。

## 4. 状态面板 & 日常操作

托盘图标点开是状态面板，含：
- **小红书登录 / 云端连接 / 会话状态 / 风控状态 / 边缘进程** 五个状态徽标。
- **浏览器**设置：切换 AdsPower / 本机 Chrome、检测状态、点选浏览器环境（AdsPower）、「高级设置」里填 API 地址 / Key。**无独立「保存」按钮**——设置在你点「启动 / 按新设置重启」时自动保存。
- **右下角悬浮按钮**：随状态三态——停止时是「**启动**」（会先保存再启动）、运行时是「**暂停**」、已暂停时是「**恢复**」。
- **重新登录**：登录态失效或 AdsPower 环境刚登录后，重启边缘流程重试。

正常运行时面板各项应为「已连接 / 运行中」。副标题会随当前 provider 说明当前用的是 AdsPower 还是本机 Chrome。

## 5. 排查

- **面板「边缘进程」显示「异常」或「云端连接」停在「未连接」**：边缘进程可能已退出或连云失败。本应用在**异常退出 / 浏览器缺失 / 连云失败**时会主动把窗口弹到前台 + 发系统通知——看到通知说明确实出问题了，不是「在后台默默跑」。按提示处理后点「重新登录」或重启应用。
- **AdsPower 模式起不来**：确认 ① AdsPower 客户端已运行、② 本地 API 已开启、③ 分身 ID 正确、④ 该分身已登录小红书。分身未登录会导致核心诚实非零退出（不会以本机指纹偷偷起跑）——在 AdsPower 窗口登录后点「重新登录」。
- **「AdsPower 状态」显示未就绪 / 环境列表刷不出来**：先启动 AdsPower 客户端并在其设置里开启本地 API，再点「检测」；若「已就绪」但「刷新」失败且提示疑似 API 校验，把「API Key」填对再点「刷新」（填了当次即生效、无需先保存）。实在拉不到，直接在「分身 ID」手填该分身的 **user_id** 兜底。
- **「缺少 Chrome」（本机 Chrome 模式）**：装 Google Chrome 后重启应用；或改用 AdsPower 模式。
- **面板「待配置」**：AdsPower 模式没填分身 ID——在「浏览器」设置里填好再「保存并启动」。
- **连不上云端**：确认本机能访问云端地址、云端在线。应用是「快速失败 + 弹窗告知」，**不会无限重连假装在跑**；处理后重启即可。
- **保存设置提示「写入本地失败」**：设置本次已生效，但没能写盘（如目录只读 / 磁盘满），**重启应用后会丢失**——按提示修好磁盘 / 权限后重新保存。
- **崩溃后看不到原因**：打开窗口，日志区最近一条即最后的状态 / 错误行。

## 6. 已知限制（当前版本）

- 未做代码签名 / 公证（首次需手动放行，见 §1）。
- 未做自动更新：升级 = 重新下载安装新版本。
- 桌面外壳多开隔离未做（见 §3），同机并行多账号走命令行多节点启动器。
- 本机 Chrome 模式不打包 Chrome（依赖系统 Chrome，见 §0）；AdsPower 模式依赖本机 AdsPower 客户端。
