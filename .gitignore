node_modules/
dist/
dist-electron/
*.log
.env
.DS_Store
edge_output.txt
tmp-cdp-query.cjs
tmp_diag.ts

# self-contained-ads-runtime: baked key (secret) + staging artifacts — do not commit
resources/ads-runtime.json
build/ads-prefix/
build/ads-runtime/
build/ads-install.log
build/native-page-engine/
build/gost/
native/page-engine/target/

# 并行开发 worktree 会把 node_modules 软链到主 checkout 复用依赖。
# 上面的 `node_modules/` 带斜杠、只匹配目录——软链在 git 眼里是符号链接文件、匹配不上，
# 于是一次 `git add -A` 就会把软链提交进仓，ff 合回 master 时顶掉真正的 node_modules 目录。
# 这一条不带斜杠，目录与软链都忽略。
node_modules
